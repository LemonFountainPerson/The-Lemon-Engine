To run or compile on Debian-based Linux (Mint, ubuntu, Pop OS, etc.) run 'install_Linux' as a program to install necessary libraries. 
or simply run this command in the terminal:
chmod +x install_Linux; ./install_Linux